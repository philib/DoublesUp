import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#d90e29',
    },
    secondary: {
      main: '#000000',
    },
    background: {
      default: '#ffffff',
    },
  },
});
